{
  "name": "wuqiongyun-idc",
  "private": true,
  "scripts": {
    "dev:api": "npm run dev:api:unix --prefix idc-finance",
    "dev:api:mysql": "npm run dev:api:mysql:unix --prefix idc-finance",
    "dev:admin": "npm run dev:admin --prefix idc-finance",
    "dev:portal": "npm run dev:portal --prefix idc-finance",
    "build:admin": "npm run build:admin --prefix idc-finance",
    "build:portal": "npm run build:portal --prefix idc-finance",
    "db:prepare:mysql": "npm run db:prepare:mysql:unix --prefix idc-finance"
  }
}
